import React, {useState, useMemo} from 'react';

const Calculator = ({}) => {
    return(
        <div style={{width: '10rem', height: '10rem'}}>
            Calculator Box
        </div>
    )
}

export default Calculator;